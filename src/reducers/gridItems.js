
//extract to actionTypes 
const ITEM_HEIGHTS = 'ITEM_HEIGHTS';

//initial state 
const INITIAL_STATE = {
  heights: [],	
};

//initial state
function gridItemsReducer(state=INITIAL_STATE, action) {
  switch(action.type) {
  	default : return state;
  }	
}

export default gridItemsReducer;